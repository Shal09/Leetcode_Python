class Solution(object):
    def countKeyChanges(self, s):
        """
        :type s: str
        :rtype: int
        """
        count = 0
        for ch in range(1,len(s)):
            if s[ch].lower() != s[ch-1].lower():
                count += 1
        return count
